<?xml version="1.0" encoding="UTF-8"?>
<section version="5.0" xmlns="http://docbook.org/ns/docbook"
         xmlns:xl="http://www.w3.org/1999/xlink"
         xmlns:xi="http://www.w3.org/2001/XInclude"
         xmlns:xhtml="http://www.w3.org/1999/xhtml"
         xmlns:svg="http://www.w3.org/2000/svg"
         xmlns:ns="http://docbook.org/ns/docbook"
         xmlns:mathml="http://www.w3.org/1998/Math/MathML">
    <title>Standard View Helper Library</title>

    <para>Should be autogenerated from the tags.</para>

    <section>
      <title>f:if</title>

      <para>This view helper implements an if/else condition.</para>

      <example>
        <title>Basic usage</title>

        <programlisting>&lt;f:if condition="somecondition"&gt;
  This is being shown in case the condition matches
&lt;/f:if&gt;</programlisting>

        <para>Everything inside the <code>&lt;f:if&gt;</code> tag is being
        displayed if the condition evaluates to TRUE.</para>
      </example>

      <example>
        <title>If / Then / Else</title>

        <programlisting>&lt;f3:if condition="somecondition"&gt;
  &lt;f3:then&gt;
    This is being shown in case the condition matches.
  &lt;/f3:then&gt;
  &lt;f3:else&gt;
    This is being displayed in case the condition evaluates to FALSE.
  &lt;/f3:else&gt;
&lt;/f3:if&gt;</programlisting>

        <para>Everything inside the "then" tag is displayed if the condition
        evaluates to TRUE. Otherwise, everything inside the "else"-tag is
        </para>
      </example>

      <table>
        <title>Arguments</title>

        <tgroup cols="4">
          <thead>
            <row>
              <entry align="center">Name</entry>

              <entry align="center">Type </entry>

              <entry align="center">Required / Default</entry>

              <entry align="center">Description</entry>
            </row>
          </thead>

          <tbody>
            <row>
              <entry>condition</entry>

              <entry>Raw</entry>

              <entry>TRUE</entry>

              <entry>If TRUE, the "then"-part is rendered, otherwise, the
              "else"-part is rendered</entry>
            </row>
          </tbody>
        </tgroup>
      </table>
    </section>
  </section>
